import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Allow uploading property photos from any source
    remotePatterns: [
      {
        protocol: "https",
        hostname: "**",
      },
    ],
  },

  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          {
            key: "X-Content-Type-Options",
            value: "nosniff",
          },
          {
            key: "X-Frame-Options",
            value: "DENY",
          },
          {
            key: "Referrer-Policy",
            value: "strict-origin-when-cross-origin",
          },
        ],
      },
    ];
  },

  // Enable React strict mode for better a11y warnings
  reactStrictMode: true,
};

export default nextConfig;
